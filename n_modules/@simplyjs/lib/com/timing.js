//timing utility

import { ticker, Ticker } from './timing/ticker.js';
import { delay, debounce, throttle, defer } from './timing/operations.js';

export default {
	ticker, Ticker,
	delay, debounce, throttle, defer
}

export {
	ticker, Ticker,
	delay, debounce, throttle, defer
}